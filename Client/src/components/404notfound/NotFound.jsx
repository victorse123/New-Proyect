import React from 'react';
const NotFound = () =>{
    // if (pathname !== "/" && pathname !== "/home" && pathname !=="/About" && pathname !== '/Favorites' && pathname !== "/Details"  ){
    return(
        <div>
            <h1>
                404 - not Found😢
            </h1>
            <p>lo sentimos, la pagina que estas buscado no existe.</p>
        </div>
            
    )
}
//1-833-578-0158
export default NotFound;